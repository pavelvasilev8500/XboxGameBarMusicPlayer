<UserControl
    x:Class="XboxGameBarMusicPlayer.Controls.ProgressRingControl"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:XboxGameBarMusicPlayer.Controls"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    d:DesignHeight="300"
    d:DesignWidth="400">

    <Grid x:Name="Root" Width="55" Height="55">
        <Ellipse Stroke="#333" StrokeThickness="5"/>
        <Path x:Name="ProgressPath"
              Stroke="#4CAF50"
              StrokeThickness="8"
              StrokeStartLineCap="Round"
              StrokeEndLineCap="Round"/>
        <Image x:Name="InnerImage"
               Source="{x:Bind InnerImageSource, Mode=OneWay}"
               Width="80" Height="80"
               HorizontalAlignment="Center"
               VerticalAlignment="Center"/>
        <Rectangle Fill="Transparent"
                   Tapped="OnTapped"/>
    </Grid>
</UserControl>
